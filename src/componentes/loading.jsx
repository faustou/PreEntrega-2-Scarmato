import { LoadingOutlined } from '@ant-design/icons';
import React from 'react';

const Loading = () => {
    return (
        <LoadingOutlined spin style={{ fontSize: '4rem', color: '#85586F', marginBottom: '3rem' }} />
    );
};

export default Loading;